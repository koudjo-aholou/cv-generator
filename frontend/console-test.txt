// Tape ces commandes dans la console du navigateur (F12) sur http://localhost:8080

// 1. Vérifier si le composant existe
document.querySelector('file-uploader')

// 2. Vérifier son contenu
document.querySelector('file-uploader').innerHTML

// 3. Vérifier son style
getComputedStyle(document.querySelector('file-uploader')).display

// 4. Vérifier la hauteur
document.querySelector('file-uploader').offsetHeight

// 5. Vérifier si customElements est défini
customElements.get('file-uploader')

// 6. Forcer un render manuel
document.querySelector('file-uploader').render()
